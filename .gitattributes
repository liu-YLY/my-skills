# 统一换行符：仓库内一律存储 LF，检出也用 LF
# 根因：Windows 环境下 CRLF 曾混入提交，触发 yamllint new-lines 报错（PR #61）
* text=auto eol=lf

# Windows 批处理脚本必须 CRLF 才能正确执行
*.bat text eol=crlf
*.cmd text eol=crlf

# 二进制文件不做任何文本转换
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.woff binary
*.woff2 binary
