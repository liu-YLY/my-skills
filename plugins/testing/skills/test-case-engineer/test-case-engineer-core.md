# 测试工程师核心流程

> 本文档合并了原四阶段指令文件的核心内容，提供完整但精简的测试用例生成流程。

## 目录

- [工作流概览](#工作流概览)
- [阶段 1：理解需求](#阶段-1理解需求)
- [阶段 2：精准提取测试点](#阶段-2精准提取测试点)
- [阶段 3：编写测试用例](#阶段-3编写测试用例)
- [阶段 4：自检与补全](#阶段-4自检与补全)
- [模式切换](#模式切换)
- [评审模式](#评审模式)（详见 [knowledge/review-mode.md](knowledge/review-mode.md)）
- [反例与黑名单](#反例与黑名单)（详见 [knowledge/anti-patterns.md](knowledge/anti-patterns.md)）

## 工作流概览

```
输入：需求文档/用户描述/代码变更
  ↓
阶段 1：理解需求 → 输出：需求理解文档（结构化）
  ↓
阶段 2：提取测试点 → 输出：测试点清单（Markdown 表格）
  ↓
阶段 3：编写用例 → 输出：测试用例（通用结构化格式）
  ↓
阶段 4：自检补全 → 输出：最终用例 + 效率报告
```

**阶段间依赖**：
- 阶段 2 的输入是阶段 1 的需求理解输出
- 阶段 3 的输入是阶段 2 的测试点清单
- 阶段 4 的输入是阶段 3 的测试用例

---

## 阶段 1：理解需求

**目标**：全面透彻地理解被测功能的业务背景、技术实现和产品现状。

**输入**：需求文档/用户描述/代码变更/设计稿
**输出**：需求理解文档（结构化，包含12个字段）

### 信息收集（按优先级顺序）

| 优先级 | 手段 | 具体做法 |
|--------|------|----------|
| **P0** | 扫描项目知识库 | 列举/扫描约定目录（`docs/`、`testing-tm-cases/`），读取相关文档 |
| **P0** | **加载产品知识库** | 识别被测功能所属产品，加载 `knowledge/products/{product-id}.md`（详见下方） |
| **P0** | 搜索代码库 | 搜索相关模块的代码实现、API 定义、数据模型、校验逻辑 |
| **P0** | 阅读已有用例 | 读取同模块/相近功能的已有用例，继承风格、避免重复 |
| **P1** | 公开文档检索 | 检索第三方服务文档、API 规范、竞品实现 |
| **P1** | 阅读产品代码 | 查看控制器/服务层/校验逻辑，理解实际行为 |
| **P2** | 追问用户 | 列出具体问题清单向用户确认，不做假设 |

### 产品知识库加载（阶段 1 必做）

当被测功能可识别产品归属时，**必须加载**对应的产品知识文件：

1. **识别产品**：从以下来源推断产品ID
   - 用户明确指定（如 `使用 [产品名称] 知识库`）
   - 需求文档中的产品名称/模块名
   - 代码路径（如 `src/modules/crm/` → 产品ID `crm`）
   - API 路径（如 `/api/v1/payment/` → 产品ID `payment`）
   - 配置文件或数据库表名中的产品标识

2. **查找知识文件**：在 `knowledge/products/` 目录下查找 `{product-id}.md`
   - 精确匹配：`{product-id}.md`
   - 模糊匹配：若无精确匹配，搜索文件名包含产品关键词的文件
   - 索引文件：参考 `knowledge/products/README.md` 中的产品列表

3. **加载并应用**：
   - 读取「业务规则与约束」→ 用于阶段 1 需求理解，提取产品特定的业务逻辑
   - 读取「高频缺陷模式」→ 用于阶段 2 补充防御性测试点，避免已知缺陷
   - 读取「专项测试检查清单」→ 用于阶段 2 确保重点领域覆盖
   - 读取「状态流转规则」→ 用于阶段 2 设计状态相关测试点
   - 读取「常见测试场景模板」→ 用于阶段 3 加速用例编写
   - 读取「数据字典与校验规则」→ 用于阶段 2/3 确保输入校验测试点准确

4. **加载后处理**：
   - 将产品知识中的业务规则与需求文档进行交叉验证
   - 识别产品特定的术语和概念，在后续阶段使用准确的业务语言
   - 标记产品知识中与当前需求相关的章节，便于阶段 2 快速定位

5. **若无产品知识文件**：跳过此步骤，按通用流程执行，但在阶段 1 输出中标注「未找到产品知识文件，按通用流程执行」

> **产品知识索引**：[knowledge/products/README.md](knowledge/products/README.md)
> **知识模板**：[knowledge/products/products-template.md](knowledge/products/products-template.md)

**信息来源追踪规则**：执行期间每读取/搜索一个文件或文档，立即追加到「信息来源」列表。

🔴 **CHECKPOINT · 阶段 1 完成**：需求理解输出后，必须展示给用户确认再进入阶段 2。用户确认前不得开始提取测试点。

### 阶段 1 失败模式与 Fallback

| 触发条件 | 一线修复 | 仍失败兜底 |
|----------|----------|------------|
| 项目文档缺失/不完整 | 扫描代码库提取业务逻辑，搜索注释和 README | 列出「存疑点」清单，要求用户补充确认；缺失段落回填「原文问题清单」（位置=文档名:章节，类型=缺失） |
| 代码库无权限或不可达 | 请求用户提供关键代码片段或 API 文档 | 基于需求文档推断，标注「待验证」；不可达的代码位置回填「原文问题清单」（位置=文件路径，类型=缺失） |
| 已有用例风格不一致 | 取最近 3 个模块的用例作为参考基线 | 采用通用用例格式，阶段 4 统一校验 |
| 用户无法确认存疑点 | 按最保守理解设计测试点，覆盖多种可能性 | 标记为「待确认」，用例中增加条件分支；存疑点回填「原文问题清单」（位置=对话轮次#N，类型=模糊） |

### 可测性与验收口径（阶段 1 必对照）

- **日志与追踪**：能否用 request id / trace id 唯一定位一次调用
- **可观测性**：指标、告警、链路是否与需求中的验收口径一致
- **造数与配置**：测试账号、造数接口、mock、特性开关是否齐备
- **验收定义**：除界面结果外，是否要求后端状态、对账结果、消息投递等可验证证据

### 需求理解输出模板

```markdown
## 需求理解

**功能概述**：[一句话描述功能的核心目的]

**需求追溯**：
| 条目 | 需求ID/PRD章节/Story | 备注 |
|------|----------------------|------|
| [能力点1] | [ID 或 章节号] | 若无正式 ID 则填「待补充」 |

**用户角色**：[列出所有涉及的用户类型/权限角色]

**业务规则**：
1. [从需求 + 代码 + 项目文档中提取的规则 1]
2. [规则 2]

**状态规则**：
- 初始状态：[系统/功能的初始状态]
- 状态切换：[允许的状态流转路径]
- 状态保存：[保存后的状态变化]
- 状态互斥：[互斥的状态组合]
- 非法状态：[不允许的状态流转]

**配置联动规则**：
- 上游配置：[影响当前功能的上游配置]
- 下游行为：[当前功能对下游的影响]
- 配置优先级：[多级配置的覆盖关系]
- 配置冲突：[配置冲突时的处理逻辑]

**默认值规则**：
- [字段/功能的默认值及其业务含义]

**异常规则**：
- [异常场景的处理逻辑，如网络异常、接口超时、权限不足等]

**数据规则**：
- [数据校验规则、数据一致性要求、数据流转规则]

**风险分析**：
- 高风险模块：[识别出的高风险模块及原因]
- 高回归风险：[可能影响的现有功能]
- 必须重点测试范围：[需要重点覆盖的场景]

**待确认项**：
- [需求未明确的地方，必须标注"待产品确认"或"待需求补充"]

**原文问题清单**：

> **触发时机**：阶段 1 信息收集、阶段 2 提取测试点、阶段 3 编写用例、阶段 4 自检补全时发现原文问题（模糊/矛盾/缺失/错误）均回填本清单，便于用户定位原文修订点。**阶段间累积，不另起章节**。

| 序号 | 原文位置 | 问题类型 | 问题描述 | 影响范围 | 处置 |
|------|---------|---------|---------|---------|------|
| 1 | [PRD §3.2 / 需求文档.md:L42 / src/api/xxx.py:L88 / 对话轮次#3] | 模糊/矛盾/缺失/错误 | [具体引用原文片段 + 问题说明] | [影响的测试点/用例编号] | [待产品确认 / 已按 X 理解设计 / 已在用例中标注条件分支] |

**原文位置规则**：
- 需求文档：`文件名:行号` 或 `PRD 章节`（如 `req.md:L42` / `PRD §3.2`）
- 代码：`文件路径:行号`（如 `src/api/auth.py:L88`）
- 用户口述：`对话轮次#N`（如 `对话轮次#3`）

**信息来源**：[列出本次分析实际参考了哪些文件/文档/检索结果]
```

### 业务系统识别（阶段 1 必做）

识别被测功能所属的业务系统类型，自动补充对应的核心测试维度：

| 系统类型 | 识别特征 | 核心测试维度 |
|----------|----------|--------------|
| **通知/邮件系统** | Email、Notification、Message、Subscription、Unsubscribe | 通知链路（发送/不发送/重复/失败）、用户状态（已订阅/已退订/黑名单）、配置影响（全局开关/子开关/用户偏好） |
| **状态机系统** | Enable/Disable、Toggle、Status、Publish/Draft、Active/Inactive | 初始状态、状态切换、状态保存、刷新后状态、状态互斥、非法状态流转 |
| **配置系统** | 全局配置、子配置、Feature Flag、多级配置 | 上游配置影响、下游行为变化、配置覆盖关系、配置优先级、配置冲突 |
| **支付/订单系统** | Payment、Order、Refund、Invoice | 金额精度、重复支付、部分退款、支付超时、订单状态流转 |
| **权限系统** | Role、Permission、Auth、Access Control | 角色权限矩阵、越权访问、Token 过期、敏感数据保护 |
| **搜索/筛选系统** | Search、Filter、Sort、Pagination | 特殊字符、空结果、大结果集、排序稳定性、筛选组合 |
| **异步任务系统** | Job、Queue、Scheduler、Cron、Async | 任务触发、重试逻辑、死信处理、延迟执行、并发控制 |

**识别后处理**：
1. 在需求理解中标注识别出的系统类型
2. 自动补充对应的核心测试维度到测试点清单
3. 若涉及多个系统，逐一覆盖各系统的核心维度

---

## 阶段 2：精准提取测试点

**目标**：从需求理解中系统性提取所有可测试点，做到不遗漏。

**输入**：阶段 1 的需求理解文档
**输出**：测试点清单（Markdown 表格，包含编号/测试点/维度/设计方法/优先级/用例数/需求引用/来源）

### 风险与优先级（粗分级）

在展开 7 维度扫描前，先对本次功能做粗粒度风险评估：变更面（影响模块多少）、业务影响（资金/合规/核心路径）、技术复杂度（新依赖、并发、分布式）。据此辅助映射 P0–P3。

### 7 维度扫描框架

对每个功能/页面，按以下 7 个维度逐一扫描：

| 维度 | 检查内容 | 最低覆盖深度 |
|------|----------|-------------|
| **正向功能** | 核心业务流程的正常路径 | 每个用户角色至少 1 条 Happy Path |
| **反向校验** | 每个输入字段的异常/非法/边界输入 | 每个字段至少 4 类：有效值、空值、格式错误、边界值 |
| **权限/角色** | 不同用户类型看到的不同内容和可用操作 | 每个角色 × 每个功能点 = 1 条 |
| **状态联动** | 选项 A 变化时选项 B 的联动响应 | 每对联动关系 × 源状态数 |
| **边界与限制** | 数量上限、长度限制、重复校验 | 上限值、上限+1、重复值、零值 |
| **UI 交互** | 按钮状态、提示文案、页面跳转、加载状态 | 每个按钮/状态变化点 = 1 条 |
| **异常与容错** | 网络异常、并发操作、服务端错误响应 | 网络断开、服务端 5xx、并发冲突 |

### 测试点设计原则（强制）

**单一验证目标**：
- 每条测试点仅允许一个核心验证目标
- 禁止：`验证保存成功并且页面展示正确`
- 必须：拆分为 `验证保存成功` + `验证页面展示正确` 两条测试点

**禁止堆叠验证**：
- 禁止在一个测试点中同时验证：
  - 正常 + 异常
  - 默认值 + 修改后
  - UI + 数据保存
  - 权限 + 展示

**要素完备性**：
- 每个测试点必须包含：[验证对象] + [操作/场景] + [预期结果方向]
- 禁止：`验证 General updates 可以关闭`（太模糊）
- 必须：`验证 General updates 状态开关(Toggle)切换至关闭并保存后，配置项保持关闭状态`

**核心业务优先**：
- 测试点数量与覆盖应向核心业务倾斜
- 禁止大量生成低风险 UI 测试点
- 例如通知系统的核心是：是否发送、是否正确发送、是否受退订影响
- 而不是：Toggle UI、文案展示

### 缺陷模式对照补充（必须执行）

7 维度扫描完成后，执行两轮缺陷模式对照：

**第一轮：通用缺陷模式**

读取 [knowledge/bug-patterns.md](knowledge/bug-patterns.md) 中的「缺陷模式→测试点映射」表，根据当前功能特征补充防御性测试点：

- 有输入框？→ 检查是否补充了 XSS、超长截断、编码问题
- 有提交按钮？→ 检查是否补充了重复提交
- 有列表页？→ 检查是否补充了空数据、分页
- 涉及权限？→ 检查是否补充了越权访问
- 有状态流转？→ 检查是否补充了状态回退、幽灵状态
- 有弹窗？→ 检查是否补充了弹窗残留

**第二轮：产品专项缺陷模式**

若阶段 1 已加载产品知识文件，读取其中的「测试重点领域」章节：

- 「历史高频缺陷」→ 对照当前功能是否涉及，补充对应测试点
- 「专项测试检查清单」→ 逐项检查是否已覆盖
- 「性能基线」→ 若涉及性能测试，参考基线值设计测试点
- 「状态流转规则」→ 确保状态相关测试点覆盖所有流转路径

### 产品特定测试点提取（阶段 2 必做）

若阶段 1 已加载产品知识文件，**必须**基于产品知识提取产品特定测试点：

1. **业务规则测试点**：
   - 从产品知识的「业务规则与约束」章节提取所有业务规则
   - 每条业务规则至少对应 1 个正向验证测试点（规则满足时的行为）
   - 每条业务规则至少对应 1 个反向验证测试点（规则违反时的错误处理）
   - 标注来源为「产品知识-业务规则」

2. **状态流转测试点**：
   - 从产品知识的「状态流转规则」章节提取所有状态流转路径
   - 每条合法流转路径对应 1 个正向测试点
   - 每条非法流转路径（不允许的状态变更）对应 1 个反向测试点
   - 覆盖所有状态组合，包括并发状态变更场景
   - 标注来源为「产品知识-状态流转」

3. **高频缺陷测试点**：
   - 从产品知识的「高频缺陷模式」章节提取历史缺陷模式
   - 若当前功能涉及相关模块/场景，补充对应的防御性测试点
   - 标注来源为「产品知识-高频缺陷」

4. **专项检查测试点**：
   - 从产品知识的「专项测试检查清单」章节提取检查项
   - 逐项检查当前功能是否涉及，涉及则补充对应测试点
   - 标注来源为「产品知识-专项检查」

5. **数据校验测试点**：
   - 从产品知识的「数据字典与校验规则」章节提取字段校验规则
   - 每个字段的校验规则对应多个测试点（有效值、无效值、边界值）
   - 标注来源为「产品知识-数据校验」

**产品特定测试点标注规则**：
- 在测试点清单的「来源」列标注「产品知识-{章节名}」
- 便于追溯测试点的来源，确保产品知识的完整应用

### 设计方法选择决策树

- 输入域有明确范围？→ 等价类划分 + 边界值分析
- 存在多个条件组合？→ 判定表/因果图
- 系统有明确状态流转？→ 状态迁移法
- 业务流程测试？→ 场景法（基本流 + 备选流 + 异常流）

### 测试点清单输出模板

```markdown
## 测试点清单

| 编号 | 测试点 | 维度 | 设计方法 | 优先级 | 用例数（估） | 需求引用 | 来源 |
|------|--------|------|----------|--------|-------------|----------|------|
| 1 | Endpoint URL 必填校验 | 反向校验 | 等价类+边界值 | P0 | 3 | PRD §3.2 | 需求 |
| 2 | XSS 防御 - URL 字段注入 | 异常与容错 | 安全模式 | P1 | 1 | - | 缺陷模式库 |

**覆盖度自检**：
- [ ] 7 个维度均已扫描
- [ ] 缺陷模式库已对照
- [ ] 每个输入字段至少覆盖：有效值、空值、格式错误、边界值
- [ ] 每个用户角色/权限组合均已覆盖
- [ ] 所有联动关系均已识别

🔴 **CHECKPOINT · 阶段 2 完成**：测试点清单输出后，必须展示给用户确认再进入阶段 3。用户确认前不得开始生成用例。

### 阶段 2 失败模式与 Fallback

| 触发条件 | 一线修复 | 仍失败兜底 |
|----------|----------|------------|
| 测试点遗漏（阶段 4 发现） | 回溯 7 维度逐项核对，补充遗漏维度的测试点 | 标记遗漏维度，用例中标注「待补充」；遗漏维度对应的需求段落回填「原文问题清单」（类型=缺失） |
| 缺陷模式库无匹配 | 使用通用缺陷模式（输入校验/状态/权限/数据/UI） | 仅输出 7 维度扫描结果，不强制补充 |
| 设计方法选择困难 | 优先使用场景法（基本流+备选流+异常流） | 多种方法并用，阶段 4 去重 |
| 需求规则表述不清导致测试点无法确定 | 列出歧义点要求用户澄清 | 按最保守理解设计测试点，歧义点回填「原文问题清单」（位置=需求文档:行号，类型=模糊） |
```

---

## 阶段 3：编写测试用例

**输入**：阶段 1 的需求理解 + 阶段 2 的测试点清单
**输出**：测试用例（通用结构化格式，Markdown 表格 + 列表）

### AI 生成模式（默认）

**流程**：
1. 读取 [knowledge/prompt-strategy.md](knowledge/prompt-strategy.md) 中的结构化提示词模板
2. 将需求理解 + 测试点清单填入 Prompt 模板
3. **若已加载产品知识**：将产品知识中的「业务规则」「常见测试场景模板」作为上下文注入 Prompt
4. AI 生成 70%-90% 的基础用例
5. **人工审核**：检查业务逻辑准确性、补充领域特有边界条件、验证优先级合理性
6. 人工补充 AI 遗漏的复杂业务逻辑用例

**产品知识注入要点**：
- 使用产品知识中的「数据校验规则」确保用例中的输入值符合业务约束
- 使用产品知识中的「状态流转规则」确保状态相关用例覆盖完整路径
- 使用产品知识中的「测试数据与环境」信息完善用例的前置条件
- 使用产品知识中的「常见测试场景模板」作为 Few-shot 示例

**AI 生成的用例标记** `source: ai-generated`，人工补充的标记 `source: manual`。

**审核重点**：
- 业务逻辑是否准确
- 领域特有边界条件是否覆盖
- 优先级是否合理
- steps 是否可直接执行

🛑 **STOP · 阶段 3 AI 生成后**：AI 生成的用例必须经过人工审核才能输出。未经审核的用例禁止标记 `source: ai-generated`。

### 阶段 3 失败模式与 Fallback

| 触发条件 | 一线修复 | 仍失败兜底 |
|----------|----------|------------|
| AI 生成质量不达标（覆盖率<80%） | 调整 Prompt：增加约束、补充 Few-shot 示例 | 分批生成（单次 10-15 条），人工补充剩余；AI 反复遗漏的测试点对应需求段落回填「原文问题清单」（类型=模糊） |
| AI 生成格式不合规 | 要求 AI 按通用用例格式重新输出 | 人工手动修正格式，标记 `source: manual` |
| steps 与 expected_results 数量不一致 | 检查每步操作是否都有可观察结果，补充或合并 | 重新梳理步骤，确保一步一验 |
| 用例标题超过 40 字符 | 尝试精简为 `被测对象 - 具体行为/条件/结果` | 若信息删除会造成同模块场景重复或无法识别环境/版本，保留长标题（禁止通过截断、`...` 或「等」隐藏关键条件） |
| 需求与代码实现矛盾（如需求说 A 代码实现 B） | 列出矛盾点要求用户澄清 | 按代码实际行为设计测试点，矛盾点回填「原文问题清单」（位置=需求:行号 + 代码:行号，类型=矛盾） |

🔴 **CHECKPOINT · 阶段 3 输出前**：在输出用例之前，必须确认以下事项：
- 所有必填字段已填写（ID、标题、优先级、类型、步骤、预期结果）
- steps 与 expected_results 数量一致（一步一验）
- 标题为 `被测对象 - 具体行为/条件/结果`，建议 40 字以内；信息删除会造成重复或无法识别环境/版本时可保留长标题
- 无禁用的模糊表达（高频 15 词：`正常`/`正确`/`符合预期`/`相关`/`对应`/`适量`/`多次`/`一些`/`大约`/`可能`/`频繁`/`边界值`/`最大`/`最小`/`等`；完整权威清单见 test-standards.md「模糊词禁用清单」）
- 用例编排符合编排规范：默认按用户旅程时序（入口 → 创建 → 查询 → 编辑 → 删除 → 恢复）；需求庞大时按功能模块分组、组内按用户旅程（详见 writing-rules.md「用例编排与排序规范」）
- 用户确认前不得输出最终用例

> **编写用例的格式、字段、铁律、拆分合并策略详见 [knowledge/writing-rules.md](knowledge/writing-rules.md)。阶段 3 编写用例时必读。**

---

## 阶段 4：自检与补全

**输入**：阶段 3 的测试用例
**输出**：最终用例 + 效率报告

**行为约束**：发现遗漏直接补充到用例；若遗漏源于原文问题（需求模糊/矛盾/缺失/错误），同时在阶段 1 输出的「原文问题清单」中记录原文位置与描述，不另起章节说明。

### 阶段 4 失败模式与 Fallback

| 触发条件 | 一线修复 | 仍失败兜底 |
|----------|----------|------------|
| 自检发现测试点遗漏 | 回溯 7 维度逐项核对，补充遗漏维度的测试点 | 标记遗漏维度，用例中标注「待补充」；遗漏对应的需求段落回填「原文问题清单」（类型=缺失） |
| 优先级比例严重偏离 | 按三步法重新评估，调整 P0-P3 分布 | 保持当前比例，在效率报告中说明偏离原因 |
| AI 生成用例覆盖率不足 | 补充 Prompt 中的测试点清单，重新生成 | 人工补充遗漏用例，标记 `source: manual` |
| 用例格式不合规 | 对照编写铁律逐项修正 | 人工手动修正，标记格式问题供后续优化 |

### 用例自检清单

```markdown
## 用例自检

**可追溯性**：
- [ ] 主要用例与测试点编号可对应
- [ ] 追溯信息（需求追溯字段或 description 首行）完整

**覆盖度检查**：
- [ ] 所有测试点均已有对应用例
- [ ] 正向 Happy Path 全覆盖
- [ ] 每个输入字段的反向校验全覆盖
- [ ] 角色/权限矩阵全覆盖
- [ ] 联动关系全覆盖
- [ ] 边界条件全覆盖

**优先级比例检查**：
- [ ] P0 占比 10%~15%
- [ ] P1 占比 30%~40%
- [ ] P2 占比 30%~40%
- [ ] P3 占比 10%~15%

**质量检查**：
- [ ] 步骤无歧义，按流程顺序编排
- [ ] 用例编排符合编排规范（用户旅程时序 / 模块分组 + 组内用户旅程），非功能类型用例独立成组置于末尾
- [ ] 包含具体输入数据和预期输出
- [ ] expected_results 无模糊描述
- [ ] steps 具体到可操作
- [ ] steps 与 expected_results 数量一致
- [ ] ID 无重复

**遗漏扫描**：
- [ ] 是否遗漏了某个字段/按钮/链接？
- [ ] 是否遗漏了某个用户角色？
- [ ] 是否遗漏了取消/返回/刷新等辅助操作？
- [ ] 涉及安全时：是否对照了安全测试检查清单？
- [ ] 源于原文问题（模糊/矛盾/缺失/错误）的遗漏是否已回填到「原文问题清单」

**产品知识覆盖检查**（若有产品知识文件）：
- [ ] 产品知识中的「历史高频缺陷」是否已覆盖
- [ ] 产品知识中的「专项测试检查清单」是否逐项检查
- [ ] 产品知识中的「状态流转规则」是否已完整覆盖
- [ ] 产品知识中的「业务规则」是否已在用例中体现

**AI 生成质量检查**（仅 AI 生成模式）：
- [ ] AI 生成用例是否覆盖了所有测试点中的标准场景
- [ ] 人工补充的用例是否覆盖了 AI 容易遗漏的领域特有业务逻辑
- [ ] AI 生成用例的 source 标记是否准确

🔴 **CHECKPOINT · 阶段 4 完成**：自检通过后，必须展示最终用例给用户确认。用户确认前不得视为完成。
```

> **效率度量报告模板与工时估算基准详见 [knowledge/writing-rules.md](knowledge/writing-rules.md)「效率度量输出」章节。**

---

## 模式切换

- **默认**：不允许跳过阶段 1/2 直接写完整用例
- **快速**：压缩阶段 1/4，但必须输出测试点清单，文首声明未做完整理解与自检
- **回归验证清单**：纯重构无行为变更时使用，跳过阶段 1/2，直接基于变更范围输出回归验证清单（无需编写完整测试用例），清单格式为：`| 序号 | 验证项 | 关联变更 | 优先级 |`
- **探索式**：额外输出探索章程，与测试点清单并列
- **评审**：独立于四阶段生成流程，对他人/已有用例做 10 维度评审（详见 [knowledge/review-mode.md](knowledge/review-mode.md)）

### 探索章程模板

```markdown
## 探索章程（Exploration Charter）

**时间盒**：[如 60min / 本迭代内]
**范围**：[功能边界 / 模块]
**关注点**：[风险区域、接口联动、竞态、边界]
**须记录证据**：[截图、日志片段、trace id、请求/响应摘录]
**退出条件**：[如发现阻塞级缺陷则停止并报告]
```

---

## 评审模式

> 评审模式独立于四阶段生成流程，对他人/已有用例做 10 维度评审，并输出评审度量报告与修订闭环。完整工作流详见 [knowledge/review-mode.md](knowledge/review-mode.md)。

## 反例与黑名单

> 基于 SkillLens 论文实证，反例黑名单提升 LLM judge 准确率。完整清单详见 [knowledge/anti-patterns.md](knowledge/anti-patterns.md)。
